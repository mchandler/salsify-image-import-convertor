# Convert Salsify Export to Import CSV

## Usage

```
npm install
node transform-images.js <input.csv>
```

Output is written to `<input>-output.csv` in the current directory.

## Windows PowerShell

Same commands. Requires Node 16+.

```
npm install
node transform-images.js .\input.csv
```
